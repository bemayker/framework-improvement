# Decisions, TEST-07

- 2026-09-03 [TEST-07] Enter the lifecycle at 6.1 (plan) as a fresh run, in worktree `.claude/worktrees/TEST-07` on `feature/TEST-07-uptime-endpoint` created from `origin/main` with `--no-track`. Rationale: the tracker item is `to do` with no branch, PR, artifacts or comments, so nothing exists to resume, and the scaffold gate passes (TEST-01 done).
- 2026-09-03 [TEST-07] No serialization hold against TEST-06 despite the shared-risk flag on `backend/app/main.py`. Rationale: TEST-06 is merged (PR #23, 8794a99), so its echo router registration is already on `main` and TEST-07 registers its router on top of it rather than beside it.
- 2026-09-03 [TEST-07] Plan self-approved (VERDICT: approve, orchestrator, run deliver-20260903T125824Z). Rationale: all four acceptance criteria carry a covering tier with a reason (1 and 2 Integration, 3 and 4 Unit, `testing_standards.md` Sections 4 and 6); no E2E spec because no criterion needs navigation or interaction; scope is the four criteria and nothing more (`user_story_alignment.md` Section 3); Router → Service → Schema with no repository, stated explicitly since nothing is persisted (`coding_standards.md` Section 2.2); `## Technology Selection` has a line for every net-new module and no new dependency; manifest validator exit 0; every codebase claim (version/echo/health precedents, conftest `client` fixture, monkeypatch-by-name pattern, README/DEVELOPMENT list no `/api/` path) verified against the worktree at 8794a99.
- 2026-09-03 [TEST-07] Planner readings accepted as recorded assumptions, not revised: `started_at` captured at import of `uptime_service` (process start, one value per process, `lifespan` untouched), `uptime_seconds` as a `time.monotonic()` delta, "explicit offset" as `+00:00` via `field_serializer`. Rationale: each is reversible, recorded with its rejected alternatives, and satisfies its criterion; a different choice would be preference, which is not a revise ground.
- 2026-09-03 [TEST-07] Residual doubt for the builder, not a re-plan: the plan asserts `components.schemas.UptimeResponse.properties.started_at.format == "date-time"` (API Contract, `test_uptime_integration.py`, UAT criterion 4 step 3), but FastAPI emits response schemas in Pydantic serialization mode, and a `@field_serializer` annotated `-> str` replaces the field's JSON schema with the return type's, so `format` is likely absent. Rationale: no acceptance criterion names `format: date-time`; the builder should drop the `-> str` annotation (or use `return_type=Any`) so the validation schema is kept, or drop that one assertion, and keep the `$ref`, `minimum: 0` and `required` assertions that do carry criteria 2 and 4.
- 2026-09-03 [TEST-07] Phases A, C and D not dispatched; Section 15 documentation check not dispatched. Rationale: the plan has no frontend work, no external API integration, tiers no criterion at E2E (an API-only feature verified by unit and integration tests, `testing_standards.md` Section 6), and states that no `README.md`/`docs/DEVELOPMENT.md` edit is warranted; dispatching a builder to conclude "skip" would change nothing.
- 2026-09-03 [TEST-07] The `format: date-time` doubt from the plan verdict was settled by measurement in Phase B: the `started_at` field serializer carries no return annotation, which keeps `format: date-time` in the serialization-mode schema while still emitting `+00:00`; the serializer docstring pins the omission and a unit test (`test_started_at_serialization_schema_keeps_the_date_time_format`, one test beyond the manifest) fails loudly if the default changes. Rationale: an implicit default relied on is made explicit rather than left to a comment (`agents/builder.md` item 6).
- 2026-09-03 [TEST-07] Phase B deviation accepted: the service unit tests pin `_STARTED_MONOTONIC` to `100.0` beside the monkeypatched `monotonic`. Rationale: with the real reference (~10^5 to 10^6 seconds since boot) the plan's exact-equality assertions (`== 12.5`, "exactly 1.0 larger") are not exact in double precision and would be flaky; pinning both makes the arithmetic exact.
- 2026-09-03 [TEST-07] Refactor gate (Phase F) re-dispatched once after the first dispatch terminated on an API 529 Overloaded error before writing or committing anything (worktree verified clean). Rationale: build-feature Section 6 recovery rule, the commit is absent so the phase is re-run; the open 6.5 window was closed and re-opened so the retry is counted rather than absorbed.
- 2026-09-03 [TEST-07] Refactor gate retry also terminated on API 529 Overloaded before any write; worktree still clean at c974978. Rationale: server-side capacity, not a defect in the item; the gate is re-dispatched after a short wait, never on a different model tier (the builder pin is agents/builder.md's alone).
- 2026-09-03 [TEST-07] Refactor gate third dispatch terminated on API 429 (individual spend limit) mid-phase, before its commit; re-dispatched after the limit reset, reconciling any uncommitted edits it left. Rationale: build-feature Section 6 recovery, the commit is the unit of progress.
- 2026-09-03 [TEST-07] Refactor gate fourth dispatch terminated on API 529 Overloaded. Rationale: capacity, not the item; backing off five minutes before the next attempt, still on the builder pin.
- 2026-09-03 [TEST-07] Pushed via GitHub MCP push_files in two commits (code/tests/UAT at abb4c58, artifacts at 34855f9) after the push gate first blocked on a missing frontend node_modules in the worktree (fixed with npm ci, not by weakening the gate); remote content verified byte-identical to the worktree. Draft PR #26 opened; tracker link comment posted. Rationale: autonomy.md Section 6, all remote git through the MCP.
- 2026-09-03 [TEST-07] CI green on PR #26 (7 of 7 checks, first run, no fix cycle). Handover rebuild FAILED in 10s: `status=failed compose=docker-compose.yml sha=6b0ee67+dirty services=none health=none reason=build-failed`, root cause `Bind for 0.0.0.0:5183 failed: port is already allocated` (the compose stack built from main, TEST-06 handover, still holds 5183/8010/5442). Handover not withheld per deliver 6.7 step 5; the failure is named in the PR body. Review-comment loop: 0 PR comments, 0 PR review threads, 0 reviews; tracker carries only the framework link comment. Rationale: a tester told the rebuild failed is better off than one handed a green PR and a stale image.
- 2026-09-03 [TEST-07] Merge verdict: MERGE PR #26 (head 34855f9, base main at 8794a99, squash). Rationale: `autonomy.md` Section 5 holds on all five conditions, each re-read from the source rather than taken from the dispatch: (1) reviewer `VERDICT: PASS blocking=0 recommended=1 optional=2`, the RECOMMENDED finding applied in 6b0ee67 and the gate added no source or test file; (2) 8 check runs on 34855f9 all `completed`/`success` (the two `notify` runs are one per workflow run), PR is `draft=false`, `mergeable_state=clean`; (3) all four criteria are implemented (`Field(ge=0)`, `AwareDatetime` + `+00:00` serializer, import-time `STARTED_AT`, `response_model=UptimeResponse`) and covered at the tier the plan's coverage table names, with unit 50/0 and integration 29/0 observed at Phases B and F in this run; (4) 0 PR reviews, 0 review threads, 0 conversation comments, and the tracker item carries only the framework's own link comment, so nothing is left unanswered on either surface; (5) additive change, one new router plus one `include_router` line, reversible by revert, no migration, secret or external side effect. The failed handover rebuild (port 5183 held by the main-built compose stack) is not a Section 5 condition and does not withhold the merge.
- 2026-09-03 [TEST-07] Two observations recorded at the merge verdict, neither a merge condition: the worktree's `CLAUDE.md` still reads `Autonomy: assisted` because the branch forked at 8794a99, one commit before e28714e flipped main to `autonomous`; the PR does not touch `CLAUDE.md`, so the squash leaves main's `autonomous` intact and no rebase is needed. `decisions.md` and `review_scope.md` show as untracked in the worktree yet are present in the PR at 34855f9, which is the expected shape of a `push_files` push (`work_items.md` Section 9: `decisions.md` has no local commit, the MCP push is its commit); this entry itself is local-only until the main session pushes it. Rationale: an auditor comparing the worktree to the PR would otherwise read both as defects.
- 2026-09-03 [TEST-07] Merged PR #26 (squash, ffbfc12). Auto-Done fired: tracker reads `complete` without a backstop write. Checkpoint suite (row flagged ✅): `verdict=pass`, 79 backend tests and 26 frontend tests on the integrated main. Standards provenance: 6 of 7 dispatches self-reported both always-on standards; 1 unbacked claim, plan.md cites mcp_integration.md which the planner did not report reading (a citation of the comment-read rule, not a standard it applied; reported, not a gate). Teardown ran `docker rm -f TEST-07-db`; the failed handover stack leftovers (test-07-* containers in Created state) are not env lines and were left in place. Rationale: deliver 6.10.
