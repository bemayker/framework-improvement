# Auto-Done pipeline (mayker-dev). Materialized from the plugin's canonical
# template (v0.3.16) by /init-project (Section 6.2). Work Item Source is
# `hybrid` and the tracker is ClickUp (secret: CLICKUP_API_KEY). On merge to
# the default branch it marks the work item Done. It runs in CI (no MCP):
#   - local item (docs/issues/{ID}.md present): flips frontmatter status to done
#     and commits it back with [skip ci]. No tracker secret needed. NOTE: this
#     pushes directly to the default branch, so if that branch is protected
#     against direct pushes, allow the github-actions bot to bypass protection
#     (or use tracker source, which transitions via API and never pushes).
#   - tracker item: transitions via the tracker REST API using a repo secret.
#
# The feature ID is parsed from the `feature/{ID}-{slug}` branch name, matched
# against the known IDs in .claude/project_state.json so IDs containing hyphens
# (e.g. US-101) parse correctly. This branch contract is enforced in-session by
# the branch-guard hook.
#
# Routing is by file presence: a docs/issues/{ID}.md wins over the tracker
# REST path. CAVEAT for hybrid twins: an item with BOTH a tracker task and a
# (shadowed) local file — e.g. TEST-02 — gets only the file flip; its ClickUp
# task must be transitioned by the session or by hand.
name: Auto-Done on merge

on:
  pull_request:
    types: [closed]

permissions:
  contents: write # for the local-source frontmatter flip commit

jobs:
  auto-done:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.ref }}

      - name: Resolve feature ID from branch
        id: fid
        env:
          BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          id=""
          if [ -f .claude/project_state.json ]; then
            best=""
            while IFS= read -r cand; do
              [ -n "$cand" ] || continue
              case "$BRANCH" in
                "feature/$cand"|"feature/$cand-"*)
                  if [ "${#cand}" -gt "${#best}" ]; then best="$cand"; fi ;;
              esac
            done < <(jq -r '(.features // {}) | keys[]' .claude/project_state.json 2>/dev/null || true)
            id="$best"
          fi
          if [ -z "$id" ]; then
            id="$(printf '%s' "$BRANCH" | sed -nE 's#^feature/([A-Za-z]+-[0-9]+)-.*#\1#p')"
          fi
          if [ -z "$id" ]; then
            echo "::warning::could not parse a feature ID from branch '$BRANCH'; nothing to do."
          fi
          echo "id=$id" >> "$GITHUB_OUTPUT"

      - name: Mark Done
        if: steps.fid.outputs.id != ''
        env:
          ID: ${{ steps.fid.outputs.id }}
          # Tracker source only. /init-project sets the secret name for your tracker
          # (CLICKUP_API_KEY / LINEAR_API_KEY / JIRA_API_TOKEN[, JIRA_EMAIL]).
          TRACKER_API_KEY: ${{ secrets.CLICKUP_API_KEY }}
        run: |
          set -euo pipefail
          issue_file="docs/issues/${ID}.md"
          if [ -f "$issue_file" ]; then
            # ---- local source: flip frontmatter, commit back (no tracker API) ----
            if grep -qiE '^status:' "$issue_file"; then
              sed -i -E 's/^[Ss]tatus:.*/status: done/' "$issue_file"
            else
              echo "::warning::$issue_file has no status: field; leaving as-is."
            fi
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add "$issue_file"
            git commit -m "chore(${ID}): mark done on merge [skip ci]" || { echo "nothing to commit"; exit 0; }
            git push
          else
            # ---- tracker source: transition via REST (example: ClickUp) ----
            if [ -z "${TRACKER_API_KEY:-}" ]; then
              echo "::error::TRACKER_API_KEY not set; cannot transition ${ID}."
              exit 1
            fi
            external_id="$(jq -r --arg id "$ID" '(.features[$id].external_id) // empty' .claude/project_state.json)"
            done_status="$(jq -r '(.status_mapping.done) // "done"' .claude/project_state.json)"
            if [ -z "$external_id" ]; then
              echo "::error::no external_id for ${ID} in project_state.json"; exit 1
            fi
            code="$(curl -sS -o /dev/null -w '%{http_code}' \
              -X PUT "https://api.clickup.com/api/v2/task/${external_id}" \
              -H "Authorization: ${TRACKER_API_KEY}" \
              -H "Content-Type: application/json" \
              -d "$(jq -nc --arg s "$done_status" '{status:$s}')")"
            if [ "$code" != "200" ]; then
              echo "::error::tracker transition failed (HTTP $code) for ${ID}"; exit 1
            fi
            echo "Transitioned ${ID} to '${done_status}'."
          fi

  validate-uat:
    # UAT Gherkin well-formedness check (mayker-dev), part of the merge pipeline.
    # Guarded on the presence of e2e/uat/scenarios/*.feature, so it is a clean
    # no-op when UAT generation is off (CLAUDE.md Feature Toggles -> UAT
    # Generation). It validates STRUCTURE only and never runs the scenarios as
    # browser tests: the executable E2E specs (*.spec.ts) run in pr-tests.yml.
    # See the plugin's DEVELOPMENT.md -> "Why UAT is validated, not executed".
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.ref }}

      - name: Validate UAT Gherkin is well-formed
        run: |
          set -euo pipefail
          shopt -s nullglob
          files=(e2e/uat/scenarios/*.feature)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No e2e/uat/scenarios/*.feature files found; UAT validation is a no-op."
            exit 0
          fi
          fail=0
          for f in "${files[@]}"; do
            errs=""
            grep -qE '^[[:space:]]*Feature:' "$f" || errs="missing 'Feature:'"
            grep -qE '^[[:space:]]*(Scenario:|Scenario Outline:)' "$f" \
              || errs="${errs:+$errs; }no 'Scenario:' or 'Scenario Outline:'"
            # Scenarios must have steps: require at least one Given/When/Then.
            grep -qE '^[[:space:]]*(Given|When|Then)\b' "$f" \
              || errs="${errs:+$errs; }no Given/When/Then steps"
            # A Scenario Outline must be backed by an Examples: table.
            if grep -qE '^[[:space:]]*Scenario Outline:' "$f" \
               && ! grep -qE '^[[:space:]]*Examples:' "$f"; then
              errs="${errs:+$errs; }Scenario Outline without an Examples: table"
            fi
            if [ -n "$errs" ]; then
              echo "::error file=$f::not well-formed Gherkin: $errs"
              fail=1
            else
              echo "ok: $f"
            fi
          done
          exit "$fail"
