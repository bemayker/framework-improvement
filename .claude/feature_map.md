# Feature Map, Dependencies

<!--
  Generated by /init-project. Work Item Source is `local`: the authoritative
  branch, depends_on, scaffold flag, and status for each item live in that
  item's docs/issues/{ID}.md frontmatter (see .claude/rules/work_items.md).
  This flat table is an informational overview of the dependency graph; if it
  ever disagrees with the frontmatter, the frontmatter wins.

  Readiness is computed from the graph: an item is ready when all its
  depends_on are Done (work_items.md Section 7). Independent items can run in
  parallel; there is no wave grouping.

  To update: re-run /init-project or edit the docs/issues/ frontmatter (and
  mirror the change here).
-->

| Feature ID | Title | depends_on | branch | scaffold | shared_risk_notes |
| --- | --- | --- | --- | --- | --- |
| TEST-01 | Static landing page | [] | feature/TEST-01-static-landing-page | ✅ | |
| TEST-02 | Health endpoint | [TEST-01] | feature/TEST-02-health-endpoint | | |
| TEST-03 | Simple note form | [TEST-01] | feature/TEST-03-simple-note-form | | ⚠️ both TEST-02 and TEST-03 touch the FastAPI app entry (router registration); serialize if run concurrently |

Scaffold gate: TEST-01 (`scaffold: true` in its frontmatter) must be Done before TEST-02 or TEST-03 can start; both then become ready simultaneously and are otherwise independent.
