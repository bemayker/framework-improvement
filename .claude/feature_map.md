# Feature Map, Dependencies

<!--
  This file is generated by /init-project (or by /deliver on its first
  autonomous run). It is a flat dependency table for all features: one row per
  item with depends_on, branch, the scaffold flag, and shared_risk_notes.
  Agents read this to determine what blocks their feature.

  Readiness is computed from the graph: an item is ready when all its
  depends_on are Done (see .claude/rules/work_items.md Section 7). Independent
  items can run in parallel; there is no wave grouping.

  Status lives outside this file: in the issue tracker via MCP for
  `tracker`/`hybrid` source, or in each work item's frontmatter under
  docs/issues/ for `local` source. With `local` source this file is flat
  (or absent); local items carry their own branch and depends_on instead.

  To update: re-run /init-project, re-run /deliver, or edit manually.
-->

> **This file will be populated when you run `/init-project` (or `/deliver` in autonomous mode).**
