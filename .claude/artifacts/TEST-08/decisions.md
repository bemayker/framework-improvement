- 2026-09-14 [TEST-08] Plan self-approved by the orchestrator (no re-plan). Rationale: every criterion mapped with criterion 2 explicitly marked superseded by the tracker correction; the refusal to add GET /api/version verified against origin/main (TEST-05 shipped router, schema, service and registration); scope contained; Criterion coverage table complete; plan-manifest-validate exit 0.
- 2026-09-14 [TEST-08] Acceptance criterion 2 treated as SUPERSEDED by the tracker correction comment. Rationale: the comment requires the version to come from the backend at runtime, not from package metadata at build time, because a frontend rebuilt against an older backend must report the backend's version. A comment overrides the description where the two disagree.
- 2026-09-14 [TEST-08] The correction's "add a GET /api/version endpoint" was satisfied by merged code, not implemented. Rationale: TEST-05 already shipped it on main; adding it would duplicate a router on the same path. The item therefore touches no backend file, which the plan states explicitly as the comment asked.
- 2026-09-14 [TEST-08] Loading and failure states recorded as decisions rather than defaulted, as the comment demanded. Loading renders exactly "Task Notes" (the fetch settles in milliseconds and a loading word would flash; rendering nothing version-shaped is what guarantees undefined/null cannot appear). Failure renders "Task Notes · version unavailable" in its own element with the version element absent from the DOM (a bare "Task Notes" is indistinguishable from loading, and "unknown" is rejected because the backend already uses it as its own sentinel).
- 2026-09-14 [TEST-08] The backend's own "unknown" sentinel is passed through verbatim rather than mapped to the failure state. Rationale: it names a backend misconfiguration, and masking it would point the reader at the network instead. Pinned by a unit test so it cannot drift silently.
- 2026-09-14 [TEST-08] TEST-04's E2E spec and UAT artifacts were edited by this item. Rationale: necessitated, not gold-plating — they asserted the frontend package.json version this change replaces, so they go red without the re-scope. The stale 5173/8000 ports in those same files were deliberately left alone as out of scope.
- 2026-09-14 [TEST-08] Refactor gate took the code arm of RECOMMENDED 1 rather than narrowing the comment. Rationale: the 5s timeout was cleared once headers arrived, leaving the body read unguarded, so a stalled body pinned the footer in loading forever — exactly what the constant documents as impossible. An accurate disclaimer beside a reachable hang is the wrong trade when the fix is contained to one function. A stalled-body regression case was added to the existing version.test.ts.
- 2026-09-14 [TEST-08] Phase D's E2E smoke run was NOT pointed at the configured base URL 5183. Rationale: that port is held by the project's long-lived compose stack serving main, which does not contain this branch's change; running there would have measured the wrong build. A scratch static+proxy harness on host-assigned port 62910 served this branch's build against the pre-existing backend (faithful, since this item changes no backend file). 7 specs passed. Nothing was provisioned that needs teardown.
