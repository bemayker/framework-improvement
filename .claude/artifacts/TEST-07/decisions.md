- 2026-09-14 [TEST-07] Plan self-approved by the orchestrator (no re-plan). Rationale: all four criteria mapped with covering tiers and per-criterion non-E2E reasons; every reused symbol verified present on origin/main; plan-manifest-validate exit 0.
- 2026-09-14 [TEST-07] Accepted the recorded coding_standards.md Section 2.2 deviation (no service module). Rationale: Section 2.2 scopes the service layer to business logic and transactional boundaries and a clock subtraction is neither; TEST-06 and FEAT-1 are the live precedent on main. An earlier run of this item did carry a service module; this one deliberately does not.
- 2026-09-14 [TEST-07] Accepted import-time capture of STARTED_AT as "captured once at application startup". Rationale: backend/Dockerfile launches one uvicorn process with --factory, no --workers and no --reload, so module import is process start. Recorded residual: a multi-worker or preforking launch would give each child its own constants and falsify the observable half of criterion 3.
- 2026-09-14 [TEST-07] Build held until FEAT-1 merged to origin/main, then the branch was reconciled with main before Phase B. Rationale: both items register a router in backend/app/main.py and edit backend/tests/unit/test_main_unit.py (whole-manifest union overlap); reconciling first meant the plan's primary insertion points applied rather than its fallback, and merged-since-check then reported verdict=clean against base ed48ef9.
- 2026-09-14 [TEST-07] Refactor gate corrected a false claim made to human testers rather than changing code to match it. The UAT script told testers that UptimeResponse documents started_at with format date-time; the generated schema shows only {"type": "string"}, because the field_serializer's `-> str` return annotation is what pydantic derives the serialisation-mode schema from. Both script copies were corrected. The serializer was NOT rewritten to satisfy the document: the wire format is already correct and two tests pin it.
- 2026-09-14 [TEST-07] Refactor gate changed the unit tier's identity assertion to equality. Rationale: `is` passed only because pydantic 2.13.4 happens to pass an already-aware datetime through uncopied; equality against the import-time constant proves the same "captured once" property without coupling the test to a library internal.
