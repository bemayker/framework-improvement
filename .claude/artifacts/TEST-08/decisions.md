# Decisions, TEST-08

- 2026-09-03 [TEST-08] Treated the item's framework status as `plan_review`, not the `in_review` the reverse status map returns for the tracker's `to test`. Rationale: `to test` is shared by `plan_review` and `in_review` (a tolerated collision), and the ground truth disambiguates it: draft PR #25 (label `plan-review`, head 207afc1) carries only `plan.md` and `shared_risks.md` with no implementation commit, so the item is ready under the readiness rule and would be wrongly excluded as "in review".
- 2026-09-03 [TEST-08] Resume at 6.2 (plan self-approval on the existing plan) rather than re-planning. Rationale: `.claude/artifacts/TEST-08/plan.md` exists on the branch, was produced by an assisted re-plan today that already incorporated the item's one actionable tracker comment (version from the backend `GET /api/version` at runtime, loading and failure states recorded), and a re-dispatch never regenerates an existing plan absent an explicit re-plan.
- 2026-09-03 [TEST-08] Reuse branch `feature/TEST-08-footer-app-version`, worktree `.claude/worktrees/TEST-08` and draft PR #25 (via `update_pull_request` at 6.6), and post no new tracker link comment. Rationale: all four already exist for this branch and PR URL; a second PR or a second link comment is the duplicate-mutation failure idempotent resume exists to prevent.
- 2026-09-03 [TEST-08] Plan self-approved on the first check (no re-plan requested). Rationale: all four acceptance criteria are mapped, criterion 2 as overridden by the tracker comment; each of the comment's demands (runtime source, endpoint, loading state, failure state, backend change stated) is recorded in `## Re-Plan Feedback`; `GET /api/version` exists on the branch from TEST-05 (`backend/app/routers/version.py`, registered in `main.py`, CORS allows GET from :5183), so no endpoint is invented; every modified path exists and every new path is credibly new; the `### Criterion coverage` table carries a tier per criterion with a reason on each non-E2E row (`testing_standards.md` Sections 4 and 6); `## Technology Selection` has a line for every net-new module and records `page.route` over a mock server and `useState`/`useEffect` over a data-fetching library; `plan-manifest-validate.sh -q` exits 0.
- 2026-09-03 [TEST-08] Judged the two scope extensions necessary consequences, not gold plating (`user_story_alignment.md` Section 3). Rationale: TEST-04's E2E spec asserts the footer contains `frontend/package.json`'s `0.0.0` while the backend reports `0.1.0`, so leaving it untouched ships a red CI on the first push, and its UAT artifacts encode the same overridden contract (edit limited to the version-source lines, stale ports deliberately left alone); extracting `frontend/src/api/http.ts` is the smallest change that keeps the `http://localhost:8010` default a one-source deployment-dependent value (`coding_standards.md` Section 5) instead of duplicating it in `version.ts`, and `notes.ts` keeps its exported behaviour.
- 2026-09-03 [TEST-08] Recorded two interpretations rather than sending the plan back. Rationale: criterion 3's "renders without it" is satisfied by the recorded failure text `Task Notes · version unavailable` because the comment explicitly asked for a recorded choice between a blank footer and a marker, and the plan records the choice and its reason; the runtime dependency on TEST-05 is real but TEST-05 is done, so the missing `depends_on [TEST-05]` edge has no scheduling effect and the dispatching session may record it in `DECISIONS.md` as an inferred dependency without touching this plan.
- 2026-09-03 [TEST-08] Phases B and C not dispatched; Section 15 documentation check not dispatched. Rationale: the plan says "No backend changes required", carries no `[C]` entry (the frontend-to-backend client `frontend/src/api/version.ts` is Phase A work), and states that build-feature Section 15's condition is not met (no structure, run configuration, dependency or test infrastructure change); dispatching a builder to conclude "skip" would cost a dispatch and change nothing.
- 2026-09-03 [TEST-08] Phase A deviation accepted: the kept contentinfo test in `AppFooter.test.tsx` became `async` and awaits the version span, to silence a React `act(...)` warning from the mount-time fetch. Rationale: same file, same case, assertion unchanged; recorded for the PR body.
- 2026-09-03 [TEST-08] Phase D smoke run used a scratch static+proxy harness on port 5184 instead of a bare Vite dev server. Rationale: `backend/app/core/config.py` fixes `cors_origins` to `http://localhost:5183` in source and that port is held by the compose stack built from main, so a 5184 origin would have been browser-blocked (the invisible CORS failure `coding_standards.md` Section 5 describes); the harness proxied `/api` same-origin, 8 of 8 specs passed, nothing outside the phase's files was touched.
- 2026-09-03 [TEST-08] Known issue recorded, not fixed in this item: `config-consistency.sh settings backend/app/core/config.py` exits 1 (`cors_origins` fixed in source while `database_url` reads the environment, `coding_standards.md` Section 5). Rationale: the file is outside this item's plan (no backend file changes) and fixing it here would be scope expansion; it goes in the PR body's known improvements and belongs to a follow-up item. Widening the literal to a list of likely ports is not the fix.
- 2026-09-03 [TEST-08] Refactor gate (Phase F) re-dispatched once after the first dispatch terminated on an API 529 Overloaded error before writing or committing anything (worktree verified clean). Rationale: build-feature Section 6 recovery rule, the commit is absent so the phase is re-run; the open 6.5 window was closed and re-opened so the retry is counted rather than absorbed.
- 2026-09-03 [TEST-08] Refactor gate retry also terminated on API 529 Overloaded before any write; worktree still clean at e2693aa. Rationale: server-side capacity, not a defect in the item; the gate is re-dispatched after a short wait, never on a different model tier.
- 2026-09-03 [TEST-08] Refactor gate third dispatch terminated on API 429 (individual spend limit) mid-phase, before its commit; re-dispatched after the limit reset, reconciling any uncommitted edits it left. Rationale: build-feature Section 6 recovery, the commit is the unit of progress.
- 2026-09-03 [TEST-08] Refactor gate fourth dispatch terminated on API 529 Overloaded. Rationale: capacity, not the item; retried with the TEST-07 gate after the back-off, uncommitted edits reconciled by the retry.
- 2026-09-03 [TEST-08] Pushed via GitHub MCP push_files in one commit (4c424ad) onto the existing plan commits; remote content verified against the worktree. Draft PR #25 reused and its body regenerated; no second tracker link comment (one already carries this PR URL). Rationale: autonomy.md Section 6 and work_items.md Section 8 idempotent resume.
- 2026-09-03 [TEST-08] CI green on PR #25 (6 of 6 checks after the build push, first run, no fix cycle). Handover rebuild FAILED in 10s: `status=failed compose=docker-compose.yml sha=60205f0+dirty services=none health=none reason=build-failed`, root cause `Bind for 0.0.0.0:5442 failed: port is already allocated` (the compose stack built from main still holds 5183/8010/5442). Handover not withheld; named in the PR body. Tracker status write to `to test` (in_review) skipped as idempotent: the item already read `to test`. Review-comment loop: 0 PR comments, 0 review threads, 0 reviews; the one actionable tracker comment (version from the backend at runtime, states recorded) was addressed in code by the plan and build and is now answered with a reply on the tracker item stating what changed. Rationale: autonomy.md Section 5 condition 4 requires tracker comments answered, never merely addressed.
- 2026-09-03 [TEST-08] Merge verdict: merge (squash) PR #25, head 4c424ad onto main. Rationale: `autonomy.md` Section 5 holds on all five conditions, verified live via the GitHub MCP rather than from the session's summary: (1) reviewer `blocking=0`, both RECOMMENDED findings applied by the gate at 60205f0 with no source or test file added; (2) 7 of 7 check runs completed green on 4c424ad (the 6 build-push checks plus `notify` after the draft conversion), PR is out of draft; (3) all four acceptance criteria covered at the plan's recorded tier and observed green this run (frontend 34/0 at A and F, e2e-smoke 8/0 at D), criterion 2 as overridden by the tracker comment; (4) 0 PR reviews, 0 review threads, 0 change requests, and the one actionable tracker comment is addressed in code and answered with a reply, which is the `answered` bar and not `resolved`; (5) frontend-only plus test and UAT edits, revertable, no migration, secret or external side effect, so the Section 4 bar is not met. Residual notes, none a merge condition: the PR touches neither `CLAUDE.md` (the worktree copy still reads `Autonomy: assisted` from base 4a1ad2c, so the squash cannot flip main's `autonomous` back), `backend/` nor `feature_map.md`; `mergeable_state` read `unknown` (GitHub's lazy compute, TEST-06 on main is disjoint by the 6.4 check); the failed handover rebuild is a local port collision and is excluded from the merge conditions by deliver 6.7 step 5; the `cors_origins` inconsistency in `backend/app/core/config.py` stays a recorded follow-up outside this item; the PR still carries the `plan-review` label, cosmetic.
- 2026-09-03 [TEST-08] Merged PR #25 (squash, a9695cc). Auto-Done fired: tracker reads `complete` without a backstop write. Checkpoint suite (row flagged ✅) run on the integrated main after both merges: see the run report for the verdict (a first run before the primary checkout had pulled a9695cc was discarded as run against the wrong base). Standards provenance: 6 of 7 dispatches self-reported both always-on standards; 1 unbacked claim, refactor_report.md cites review_standards.md which the gate builder did not report reading (it names the section the findings came from; reported, not a gate). No `- env:` line of this run, so nothing to tear down; the failed handover stack leftovers (test-08-* containers in Created state) were left in place. Rationale: deliver 6.10.
